Factory Contract
// Initialize Factory Contract
- Initialize factory owner
- Initialize list of supported token pairs (PairAddresses)

// Admin Functions
function SetOwner(new_owner):
  - Check if msg.sender is factory owner
  - Set new owner

// Create Pair
function CreatePair(tokenA, tokenB):
  - Check if token pair already exists in PairAddresses
  - Create new Pair Contract
  - Add to PairAddresses
  - Log 'PairCreated' event

// Get Pair Address
function GetPair(tokenA, tokenB):
  - Return address from PairAddresses
